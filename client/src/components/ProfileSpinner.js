import loading from '../images/loading.gif'

const ProfileSpinner = () => {

  return (
    <div className="profile-spinner text-center">
      <img id="loading" src={loading}></img>
    </div>
  )

}

export default ProfileSpinner